# Use a base image that might already be cached
FROM scratch

# Copy the pre-built binary
COPY simple-backend-linux /app/main

# Set working directory
WORKDIR /app

# Expose port
EXPOSE 8080

# Set environment
ENV PORT=8080

# Run the application
CMD ["./main"]
